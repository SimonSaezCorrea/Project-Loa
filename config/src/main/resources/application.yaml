server:
  port: 8081
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/SimonSaezCorrea/Project-Loa
          search-paths: "config-data"
